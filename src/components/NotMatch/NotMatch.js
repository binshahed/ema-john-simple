import React from "react";

const NotMatch = () => {
  return (
    <div>
      <h1 style={{ color: "gray", textAlign: "center", fontSize: "50px" }}>
        ops page NotFound!!!!
      </h1>
    </div>
  );
};

export default NotMatch;
